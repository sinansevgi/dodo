<%= form_with(model: gorev) do |form| %>
  <% if gorev.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
      <h2><%= pluralize(gorev.errors.count, "error") %> prohibited this gorev from being saved:</h2>

      <ul>
        <% gorev.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5 flex items-center gap-5">
    <%= form.label :isim %>
    <%= form.text_field :isim, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full" %>
  </div>

  <div class="my-5 flex items-center gap-5">
    <%= form.label :durum %>
    <%= form.check_box :durum, class: "block mt-2 h-5 w-5" %>
  </div>

  <div class="my-5 flex items-center gap-5">
    <%= form.label :notlar %>
    <%= form.text_area :notlar, rows: 4, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full" %>
  </div>

  <div class="my-5 flex items-center gap-5">
    <%= form.label :oncelik %>
    <%= form.select :oncelik, Gorev.onceliks.keys.map{ |key| [key.humanize, key] }, selected: gorev.oncelik %>
  </div>

  <div class="my-5 flex items-center gap-5">
    <%= form.label :sontarih %>
    <%= form.date_field :sontarih, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full" %>
  </div>

  <div class="my-5 flex items-center gap-5">
    <%= form.label :kategori %>
    <%= form.select :kategori, Gorev.kategoris.keys.map{ |key| [key.humanize, key] }, selected: gorev.kategori %>
  </div>

  <div class="inline">
    <%= form.submit "Güncelle", class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
